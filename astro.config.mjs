// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Configure for GitHub Pages. Change 'greemets-landing' if your repository is named differently.
  site: 'https://greemets.github.io',
  base: '/greemets-landing',
});
